<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkModeBackground</item>
        <item name="colorPrimaryDark">@color/darkModeBackground</item>
        <item name="colorAccent">@color/cursorColour</item>
    </style>

    <style name="Widget.RoomDbInJava.AppWidget.Container" parent="android:Widget">
        <!--        <item name="android:id">@android:id/background</item>-->
        <!--        <item name="android:background">?android:attr/colorBackground</item>-->
        <item name="android:id">@android:id/background</item>
        <item name="android:background">@color/darkModeBackground</item>

    </style>

    <style name="Widget.RoomDbInJava.AppWidget.InnerView" parent="android:Widget">
<!--        <item name="android:background">?android:attr/colorBackground</item>-->
        <item name="android:background">@color/darkModeBackground</item>
<!--        <item name="android:textColor">?android:attr/textColorPrimary</item>-->
        <item name="android:textColor">@color/darkModeBackground</item>
    </style>

</resources>